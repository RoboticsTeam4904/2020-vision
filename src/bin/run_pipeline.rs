// mod control_panel;
// mod extraction;
// mod analysis;

use vision_2020::pipeline::VisionPipeline;
use stdvis_core::types::{CameraConfig, Pose};

fn main() {
    let config = CameraConfig 
    let pipeline = VisionPipeline::new(
        camera: CameraConfig,
        extractor,
        analyzer
    )
}